// Importing the User model from the User.js file
const User = require('./User');

// Importing the Thought model from the Thought.js file
const Thought = require('./Thought');

// Exporting an object containing both models for use in other parts of the application
module.exports = { User, Thought };
